{
  "name": "chat_realtime",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend_api && npm install --prefix frontend_chat && npm run build --prefix frontend_chat && npm run build --prefix backend_api",
    "start": "npm run start --prefix backend_api"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
